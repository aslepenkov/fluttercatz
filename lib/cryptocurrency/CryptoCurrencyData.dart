class CryptoCurrencyData {
  String name;
  String symbol; //logo
  int rank;
  double price;
  String imageURL;

  CryptoCurrencyData(
      {this.name, this.symbol, this.rank, this.price, this.imageURL});
}
